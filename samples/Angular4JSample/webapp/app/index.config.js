(function ()
{
    'use strict';

    angular
        .module('appAngular')
        .config(config);

    /** @ngInject */
    function config($mdDateLocaleProvider, $httpProvider, $provide, $translateProvider){
                
    }
})();